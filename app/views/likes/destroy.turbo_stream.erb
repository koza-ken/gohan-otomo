<%= turbo_stream.replace "like_button_#{@post.id}" do %>                                                         │ │
  <%= render 'likes/button', post: @post %>                                                                      │ │
<% end %>
