<div class="min-h-screen bg-gradient-to-br from-orange-50 via-orange-100 to-yellow-50 flex items-center justify-center py-8 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md lg:max-w-6xl w-full p-6">
    <!-- 戻るリンク -->
    <div class="mb-6">
      <%= link_to "← 投稿詳細に戻る", @post,
                  class: "text-orange-500 hover:text-orange-700 font-medium inline-flex items-center" %>
    </div>
    <div class="max-w-md lg:max-w-6xl w-full bg-white rounded-3xl shadow-2xl p-6 lg:p-10 border border-orange-200">
      <div class="text-center mb-8">
        <h2 class="text-2xl font-bold text-orange-600 mb-6">お供だちの情報を編集</h2>
      </div>
      <%= render 'form', post: @post %>
    </div>
  </div>
</div>
